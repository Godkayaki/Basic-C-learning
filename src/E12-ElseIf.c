/**
godkayaki_2020
https://github.com/Godkayaki
**/
#include <stdio.h> 


int main(){
	char peli;
	int edat;

	printf("Qualificacio: ");
	scanf (" %c",&peli);
	printf("Edat: ");
	scanf (" %d",&edat);
		
	if((&peli=="M") && (edat >=13)){
		printf("Pots pasar\n");
	}else if
	((&peli=="J") && (edat >=16)){
		printf("Pots pasar\n");
	}else if
	((&peli=="A") && (edat >=18)){
		printf("Pots pasar\n");
	}else if
	(&peli=="T"){
		printf("Pots pasar\n");
	}else{
		printf("No pots pasar");
	}

	return 0;
}
